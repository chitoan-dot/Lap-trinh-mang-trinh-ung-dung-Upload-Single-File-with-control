# Desktop Upload App

Chạy server:

```bash
python upload/main.py server
```

Chạy client:

```bash
python upload/main.py client
```

Cài thư viện:

```bash
pip install customtkinter pillow tkinterdnd2
```
